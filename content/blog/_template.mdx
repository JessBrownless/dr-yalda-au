---
# ── HOW TO USE THIS TEMPLATE ─────────────────────────────────────────────────
# Duplicate this file and drop the leading underscore, naming it for the slug:
#     cp _template.mdx natural-looking.mdx
# Files starting with "_" are ignored, so this template never publishes.
# Fill in every field below — the build will reject a post with missing or
# invalid frontmatter, and tell you exactly what's wrong.
# ─────────────────────────────────────────────────────────────────────────────

title: "Your post title in sentence case"           # required
# cardTitle: "Alternate title for index cards"       # optional — defaults to title; keep a consistent length (~50 chars) so a row of cards lines up
# slug: "custom-slug"                               # optional — defaults to the filename
description: "One or two sentences, roughly 150–160 characters. Used for the meta description, the social share preview, and (later) the index card. Be specific." # required
publishedAt: "2026-01-01"                           # required — YYYY-MM-DD
# updatedAt: "2026-01-02"                            # optional — YYYY-MM-DD
category: "philosophy"                              # required — philosophy | consultation | education | personal
# readingTime: 4                                    # optional — auto-computed from the body if omitted

# Featured image (all three optional, but featuredImageAlt is REQUIRED if featuredImage is set):
# featuredImage: "/assets/blog/your-image.avif"
# featuredImageAlt: "Plain description of the image for screen readers"
# featuredImageCaption: "Optional visible caption, written separately from the alt text"
---

{/* VOICE: first person, Australian English, restrained and editorial. No
    exclamation marks or superlatives. Don't name Schedule 4 substances (use
    "injectables" / "anti-wrinkle treatments"). Delete these examples as you go. */}

Open with a plain paragraph that sets up the piece in your own voice.

## A section heading

Body copy. Section headings get clear space above them automatically.

### A smaller sub-heading

More body copy beneath a sub-heading.

A short list:

- First point
- Second point
- Third point

> A pull-quote for a single line worth emphasising.

{/* Inline image — the bracketed text is the alt (accessibility); the text in
    quotes is the OPTIONAL visible caption. Omit the quotes for no caption. */}
![Alt text describing the image](/assets/your-image.avif "Optional visible caption")

---

A closing paragraph to round the piece off.
